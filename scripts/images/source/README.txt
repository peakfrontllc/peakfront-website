Drop AI-generated source images here before running `npm run optimize:images`.

Expected filenames (any of .webp, .jpg, .jpeg, .png):
- hero
- heavy-equipment
- transport
- buses
- power-equipment

Output is written to public/images/*.webp (same paths the site already uses).
